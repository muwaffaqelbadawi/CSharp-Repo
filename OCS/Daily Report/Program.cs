namespace dotnet // Note: actual namespace depends on the project name.
{
    /*

        This is the Main entry to the program
    
    */

    class Program // This is the name of the file
    {
        static void Main(string[] args)
        {
            // Report_Pos_Regex RPR = new Report_Pos_Regex();
            // RPR.testSub();

            // Daily_Report_Regex DRR = new Daily_Report_Regex();
            // DRR.testSub();

            // RBT_Revenue_Regex RBTRR = new RBT_Revenue_Regex();
            // RBTRR.testSub();

            // Packages_Statistcs_Regex PSR = new Packages_Statistcs_Regex();
            // PSR.testSub();

            // Revenue_Roam RR = new Revenue_Roam();
            // RR.testSub();

            // SubDailyReport SDR = new SubDailyReport();
            // SDR.testSub();

            // Revenue_Main_Account RMA = new Revenue_Main_Account();
            // RMA.testSub();

            // Packages_Active_Sub PAS = new Packages_Active_Sub();
            // PAS.testSub();

            Call_Chargeduration_Regex CCD = new Call_Chargeduration_Regex();
            CCD.testSub();
        }
    }
}