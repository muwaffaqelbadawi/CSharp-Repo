namespace dotnet // Note: actual namespace depends on the project name.
{
    /*

        This is the Main entry point to the program
    
    */

    class Program // This is the name of the file
    {
        static void Main(string[] args)
        {
            // MathTrick mT = new MathTrick();
            // mT.perfectSqrtCheck("144");

            // TestLogic tl = new TestLogic();
            // tl.LeapYear(2004);

            // SmallnBignum sn = new SmallnBignum();
            // sn.FindSmallestNum();

            // ClassificationTest CT = new ClassificationTest();
            // CT.ClassifyErr();
            // CT.Show();

            // DateTypeFormat DTF = new DateTypeFormat();
            // DTF.CalcLastDayOfTheMonth(ToDate);
            // DTF.DateFormat();

            // CreateNewDirectory CND = new CreateNewDirectory();
            // CND.SetNewDirectory();

            // Null_Coalescing_Operators NCO = new Null_Coalescing_Operators();
            // NCO.Null_Co_Operator();

            // var person = new Person
            // {
            //     // Name = "null"
            // };

            // string name = person.Name ?? "Muwaffaq";
            // Console.WriteLine(name);

            // PlayWithDataTable PWDT = new PlayWithDataTable();
            // PWDT.dt();

            // Language_Integrated_Query_LINQ LINQC = new Language_Integrated_Query_LINQ();
            // LINQC.LINQ_Func_C();


            // StringJoinMethod SJM = new StringJoinMethod();
            // SJM.JoinString();

            // CurrentUserFolder CUF = new CurrentUserFolder();
            // CUF.ThisPCFolder();
        }
    }
}